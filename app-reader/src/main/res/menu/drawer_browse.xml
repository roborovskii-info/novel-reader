<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">
    <group android:checkableBehavior="single">

        <item
                android:id="@+id/drawer_find"
                android:icon="@drawable/ic_drawer_action_find_in_page"
                android:title="@string/find_novel"/>
        <item
                android:id="@+id/drawer_read"
                android:icon="@drawable/ic_drawer_content_inbox"
                android:title="@string/read_novel"/>
        <item
                android:id="@+id/drawer_settings"
                android:icon="@drawable/ic_drawer_settings"
                android:title="@string/settings"/>
    </group>
</menu>
